%h1
  =@test.title
- @test.questions.each do |q|
  %p
    = q.id
    = q.body
    = link_to "Show", question_path(q)
    = link_to "Delete", question_path(q), method: :delete, data: {confirm: "Are you shure?"}
%p
  = link_to "New_question", new_test_question_path(@test)
